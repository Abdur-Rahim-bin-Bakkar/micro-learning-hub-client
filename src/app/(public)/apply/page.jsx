import React from 'react';
import ApplyPageContainer from './ApplyContainer';
// import getUserSessionServer from '../../lib/sessions/sesionServer/getUserSessionServer'

const ApplyPage =async () => {
    // const session = await getUserSessionServer()
    return (
        <div>
            <ApplyPageContainer/>
        </div>
    );
};

export default ApplyPage;